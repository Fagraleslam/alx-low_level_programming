#include <stdio.h>
/**
 * main - Entry Point
 *
 * Description: A Program that prints a line with printf function
 *
 * Return: 0 (Success)
 */
int main(void)
{
        puts("\"with proper grammar, but the outcome is a piece of art,\n");
        return (0);
}
